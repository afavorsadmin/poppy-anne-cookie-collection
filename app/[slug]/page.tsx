import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { seoPages } from "../../lib/seo-pages";

export function generateStaticParams(){return Object.keys(seoPages).map(slug=>({slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const page=seoPages[slug];if(!page)return{};return{title:page.title,description:page.description,alternates:{canonical:`/${slug}`}};}
export default async function SeoPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const page=seoPages[slug];if(!page)notFound();return <><div className="migration">Vercel migration preview — secure ordering remains on the live website.</div><header className="header"><Link className="brand" href="/">The Poppy-Anne Cookie Collection</Link><nav className="nav"><Link href="/">Home</Link><a href="https://thepoppyannecookiecollection.com/?page=shop">Shop</a><a href="https://thepoppyannecookiecollection.com/?page=about">About</a></nav></header><section className="seo-hero"><h1>{page.heading}</h1></section><main className="prose"><article className="card"><h2>Cookies Created Around Your Event</h2>{page.paragraphs.map(p=><p key={p}>{p}</p>)}<p>Include the occasion, colors, names, date, quantity, and special instructions with your request. You can review products and notes before secure Square checkout.</p></article><section className="card"><h2>Ready to Plan Your Order?</h2><p>Browse the live shop to view current products, variations, quantities, and prices.</p><a className="button" href="https://thepoppyannecookiecollection.com/?page=shop">Place an Order</a></section><section className="card links"><Link href="/custom-cookies">Custom Cookies</Link><Link href="/custom-cookie-orders">How to Order</Link><Link href="/special-event-cookies">Special Events</Link></section></main><footer className="footer">© The Poppy-Anne Cookie Collection LLC • Glenpool, Oklahoma</footer></>}
